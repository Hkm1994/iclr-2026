# Stub for hybrid coarse + residual head.
experiment:
  mlflow_experiment_name: gram-warped-ifw
paths:
  data_split: configs/data_split.yaml
  eval_protocol: configs/eval_protocol.yaml
train:
  model: hybrid_residual
  model_family: hybrid_residual
  lr: 0.0001
  max_train_steps: 10
  train_subsample_N: 4096
  loss_weights:
    main: 1.0
    residual: 0.5
  device: null
